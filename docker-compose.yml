version: '3.8'

services:
  pokerhub:
    build:
      context: .
      dockerfile: src/PokerHub.Web/Dockerfile
    container_name: pokerhub-web
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      # Connection string for Azure SQL (update with your credentials)
      - ConnectionStrings__DefaultConnection=Server=dentalpro.database.windows.net,1433;Initial Catalog=dentalpro;User ID=usr_admin;Password=SUA_SENHA;Encrypt=True;TrustServerCertificate=True;Connection Timeout=120;ConnectRetryCount=5;ConnectRetryInterval=10;
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

# For local development with PostgreSQL (optional - uncomment to use)
# services:
#   pokerhub:
#     build:
#       context: .
#       dockerfile: src/PokerHub.Web/Dockerfile
#     container_name: pokerhub-web
#     ports:
#       - "8080:8080"
#     environment:
#       - ASPNETCORE_ENVIRONMENT=Development
#       - ASPNETCORE_URLS=http://+:8080
#       - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=pokerhub;Username=pokerhub;Password=PokerHub@2024;CommandTimeout=120;
#     depends_on:
#       postgres:
#         condition: service_healthy
#     restart: unless-stopped
#
#   postgres:
#     image: postgres:16-alpine
#     container_name: pokerhub-postgres
#     environment:
#       POSTGRES_USER: pokerhub
#       POSTGRES_PASSWORD: PokerHub@2024
#       POSTGRES_DB: pokerhub
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     ports:
#       - "5432:5432"
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U pokerhub -d pokerhub"]
#       interval: 5s
#       timeout: 5s
#       retries: 5
#     restart: unless-stopped
#
# volumes:
#   postgres_data:
