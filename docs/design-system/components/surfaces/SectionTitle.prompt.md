**SectionTitle** — uppercase group label with optional icon + trailing action.

```jsx
<SectionTitle icon="trophy" action={<Button variant="ghost" size="sm" iconRight="chevron-right">Ver tudo</Button>}>
  Próximos torneios
</SectionTitle>
```
